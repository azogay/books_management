{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}
    <h1 class="text-center fw-bold m-4 text-primary text-uppercase">Edit Book</h1>

    {{ include('book/_form.html.twig', {'button_label': 'Update'}) }}
<div class="text-center">
    <a type="button" class="btn btn-primary col-3" href="{{ path('app_book_index') }}">back to list</a>

</div>
    
    {{ include('book/_delete_form.html.twig') }}
{% endblock %}
